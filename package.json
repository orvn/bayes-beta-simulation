{
  "name": "bayes-beta-simulation",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch run src/server.ts",
    "start": "bun run src/server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
